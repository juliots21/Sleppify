<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/llTvPlayerPane"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight="1.2"
    android:visibility="gone"
    android:layout_margin="10dp">

    <!-- Blurred Background -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/ivTvPlayerBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:alpha="0.87"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Medium" />

    <!-- Overlay for better text readability -->
    <View
        android:id="@+id/vTvPlayerOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#99000000" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
        
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/ivTvPlayerCover"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_music"
            android:elevation="12dp"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Medium" />

        <TextView
            android:id="@+id/tvTvPlayerTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Selecciona música" />

        <TextView
            android:id="@+id/tvTvPlayerSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#CCCCCC"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Sleppify TV" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:orientation="horizontal"
            android:gravity="center">
            
            <ImageButton
                android:id="@+id/btnTvPrev"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_player_skip_previous"
                android:focusable="true"
                android:nextFocusRight="@id/btnTvPlayPause"
                android:nextFocusLeft="@id/navigationRail"
                app:tint="@color/white" />

            <ImageButton
                android:id="@+id/btnTvPlayPause"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_player_play"
                android:focusable="true"
                android:nextFocusLeft="@id/btnTvPrev"
                android:nextFocusRight="@id/btnTvNext"
                app:tint="@color/white" />

            <ImageButton
                android:id="@+id/btnTvNext"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_player_skip_next"
                android:focusable="true"
                android:nextFocusLeft="@id/btnTvPlayPause"
                android:nextFocusRight="@id/rvMusicResults"
                app:tint="@color/white" />
        </LinearLayout>

        <SeekBar
            android:id="@+id/sbTvProgress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:max="1000"
            android:progress="0"
            android:focusable="true"
            android:progressTint="@color/stitch_blue_light"
            android:thumbTint="@color/stitch_blue_light" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
